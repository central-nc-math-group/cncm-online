import Redis from "ioredis";

// // remember to only use redis in a server-side context

// const redis = new Redis({ host: process.env.REDIS_HOST, port: parseInt(process.env.REDIS_PORT), password: process.env.REDIS_PASSWORD });

// export default redis;